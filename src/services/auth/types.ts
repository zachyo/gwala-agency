export type LoginResponse = {
  adminId: number;
  fullName: string;
  email: string;
  role: string;
  token: string;
};

export type User = {
  adminId: number;
  fullName: string;
  email: string;
  role: string;
};
